// Server Interface
export interface IServer {
{{#each paths as |path url|}}
  {{#each (get_methods_obj path)}}
  {{operationId}}: constructor<{{http_route_name operationId}}>;
  {{/each}}
{{/each}}
}
